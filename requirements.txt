pandas>=2.0
numpy>=1.26
# Pinned exactly: models/rail_corrugation_model.joblib was trained and pickled
# with scikit-learn 1.8.0. joblib.load() on a fitted estimator is only
# guaranteed compatible with the scikit-learn version (major.minor) it was
# saved with -- an older or newer scikit-learn here can silently fail to
# load the Rail model or change its behaviour. Retrain and re-pin together
# if this ever needs to move.
scikit-learn==1.8.0
joblib==1.5.3
matplotlib>=3.8
seaborn>=0.13
jupyter
# Shared Streamlit app (app/streamlit_app.py, app/rail_view.py): file
# upload, download buttons, st.cache_resource and colored markdown badges
# all need a reasonably recent Streamlit.
streamlit>=1.30
