# Notebooks

This folder is meant for:
- Data exploration
- Visual inspection
- Feature experimentation
- Model comparison

Use notebooks to test ideas quickly, but final reusable logic should be moved into the Python modules under `src/` rather than remaining only in a notebook.

Keep notebooks lightweight and focused on analysis. Do not treat notebooks as the final implementation location for production-ready code.
